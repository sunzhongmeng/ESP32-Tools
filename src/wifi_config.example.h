#pragma once

// Copy this file to src/wifi_config.h and fill in your own LAN Wi-Fi
// credentials. src/wifi_config.h is ignored by git to avoid publishing secrets.
#define ESP32_TOOLS_STA_SSID "YOUR_WIFI_SSID"
#define ESP32_TOOLS_STA_PASSWORD "YOUR_WIFI_PASSWORD"
